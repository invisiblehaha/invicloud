<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vision.invicloud.support.mapper.OrderProductMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.vision.invicloud.support.entity.OrderProduct">
        <result column="order_id" property="orderId" />
        <result column="product_id" property="productId" />
        <result column="buy_amount" property="buyAmount" />
        <result column="pay_amount" property="payAmount" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        order_id AS orderId, product_id AS productId, buy_amount AS buyAmount, product_amount AS productAmount
    </sql>

    <insert id="addOrderItemToDB" >
        INSERT INTO crm_order_product (
        order_id,
        product_id,
        buy_amount,
        pay_amount
           )
              VALUES   (  #{orderId},
                           #{productId},
                          #{buyAmount},
                          #{payAmount}
                        )
</insert>
</mapper>
